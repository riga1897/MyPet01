name: "[DEPRECATED] Production Deploy — merged into ci-cd.yml"

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is deprecated
        run: |
          echo "This workflow has been merged into ci-cd.yml"
          echo "Production deploys now run via ci-cd.yml on push to main branch"
          echo "Please delete this file from your repository"
          exit 1
