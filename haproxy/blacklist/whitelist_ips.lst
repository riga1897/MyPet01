# =============================================================
# HAProxy Auto-Ban Whitelist
# =============================================================
# IP-адреса в этом файле НИКОГДА не будут забанены автоматически.
# Формат: один IP на строку.
# Комментарии начинаются с #
# =============================================================

# --- Ваш домашний IP ---
95.31.25.30
109.252.112.119

# --- IP серверов ---
# VPS2 (pre-production) — добавить IP после создания сервера
# YOUR_VPS2_IP
# VPS1 (production) — добавить IP после создания сервера
# YOUR_VPS1_IP

# --- Локальные сети (не приходят извне, но на всякий случай) ---
127.0.0.1
