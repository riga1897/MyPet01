[tool.poetry]
name = "mypet01"
version = "0.1.0"
description = "Site for family"
authors = ["riga1897 <riga1897@yandex.ru>"]
readme = "README.md"
packages = [{include = "mypet_project"}]

[tool.poetry.dependencies]
python = "^3.12"
django = ">=5.1"
djangorestframework = "*"
psycopg2-binary = "*"
python-dotenv = "*"
django-environ = "^0.12.0"
pytest-cov = "^7.0.0"

[tool.poetry.group.dev.dependencies]
pytest = "*"
pytest-django = "*"
black = "*"
ruff = "*"

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "mypet_project.settings"
python_files = ["tests.py", "test_*.py", "*_tests.py"]
addopts = "--cov=mypet_project --cov-report=term-missing"

[tool.coverage.run]
omit = [
    "*/migrations/*",
    "*/__init__.py",
    "mypet_project/wsgi.py",
    "mypet_project/asgi.py",
    "manage.py",
]

[tool.coverage.report]
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if self.debug:",
    "if __name__ == .__main__.:",
    "raise AssertionError",
    "raise NotImplementedError",
]
